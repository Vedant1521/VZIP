# VZIP
VZIP is a file compression tool written in C that implement static Huffman coding . It defines its own binary format with magic bytes , builds the Huffman tree and performs bit-level encoding and decoding
